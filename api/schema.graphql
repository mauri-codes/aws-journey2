schema {
   query: Query
   mutation: Mutation
}

type Query {
   getLab(pk:ID!): Lab
}


type Mutation {
    addLab(pk: ID!, sk: String, overview: String, tests: String): Lab
}

type Lab {
   pk: ID!
   sk: String
   overview: String
   tests: String 
}
